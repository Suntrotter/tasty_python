# Tasty Python lesson CSS split

Copy `lessons.css` into your current styles folder where the old `lessons.css` lives.

Then create a subfolder named `lesson` next to it and copy all files from this `lesson` folder there.

Order matters: `lessons.css` imports base files first and final patches last.
